{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["code", "message"],
  "properties": {
    "code": { "enum": ["invalid", "denied", "missing", "conflict", "unavailable"] },
    "message": { "type": "string" }
  }
}
